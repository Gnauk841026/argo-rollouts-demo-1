# 修正後的 demo-service.yaml

# 1. 兩個 Service（canary + stable）
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-stable
spec:
  selector:
    app: rollouts-demo
  ports:
    # --- 在此處新增 name: http ---
    - name: http
      port: 80
      targetPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-canary
spec:
  selector:
    app: rollouts-demo
  ports:
    # --- 在此處新增 name: http ---
    - name: http
      port: 80
      targetPort: 8080